# Claude Code machine-local settings — may contain per-machine config.
# .claude/skills/ IS tracked: those are the Spec Kit workflow, meant to be shared.
.claude/settings.local.json
.claude/*.local.json

# Editor and OS cruft
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
